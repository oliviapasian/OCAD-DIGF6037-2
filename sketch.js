function setup() {
    createCanvas(400, 400);
    fullScreen(true);
  }
  
  function draw() {
    background(220);
    // for test file structure; delete later
    text(petName, 60, 60); 
    fill(eyeShadeColour);
    circle(120,120,60)
  
  }
